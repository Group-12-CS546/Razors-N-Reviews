<h1>Characters Found</h1>
<main>
    {{!-- <h2>{{data.name}}</h2> --}}
    {{!-- <h1>{{this.data}}</h1> --}}
    {{!-- {{#each data}} --}}
    <h1>{{this.salonId}}</h1>
    <span>{{this.name}}</span>
    <span>{{this.address}}</span>
    <span>{{this.city}}</span>
    <span>{{this.rating}}</span>
    {{!-- {{/each}} --}}


    <button type="submit" class="btn btn-primary">Post a Review</button>
    <button type="submit" class="btn btn-primary">delete</button>

    {{!-- <form method="post" action="/salons/{{salonId}}/delete?_method=DELETE">
        <input type="hidden" name="_method" value="DELETE">
        <div class="dropdown">
            <button class="dropbtn">Options</button>
            <div class="dropdown-content">
                {{!-- <p><a href="/reviews/{{this.review._id}}/edit">Edit Review</a></p> --}}
                {{!-- <p><a href="../sal/{{this.restaurant._id}}/{{this.review._id}}/delete">Delete Review</a></p> --}}
                {{!-- <p><a href="/salons/{{this.salonId}}/delete">Delete Review</a></p>
            </div>
        </div>
    </form> --}} 


    <form id="login" action="/salons/{{this.salonId}}/delete" method="POST">
        <div class="dropdown">
            <button class="dropbtn">Options</button>
            <div class="dropdown-content">
                {{!-- <p><a href="/reviews/{{this.review._id}}/edit">Edit Review</a></p>
                <p><a href="../sal/{{this.restaurant._id}}/{{this.review._id}}/delete">Delete Review</a></p> --}}
                <p><a href="/salons/{{this.salonId}}/delete">Delete Review</a></p>
            </div>
        </div>
    </form> 


    {{#if data}}
    <ul>
        {{#each data}}
        <li>
            <a style="color: white;" href="/salons/">******{{this.salonId}}</a>
        </li>
        {{/each}}
    </ul>
    {{else}}
    <p class="not-found">We're sorry, but no results were found for {{this.data.name}}.</p>
    {{/if}}


    {{#if hasErrors}}
    {{#each errors}}
    <li>
        {{this}}
    </li>
    <div class="another">
        <a href="/">Make another search</a>
    </div>
    {{/each}}
    {{/if}}
    <div class="another">
        <a href="/">Make another search</a>
    </div>

</main>