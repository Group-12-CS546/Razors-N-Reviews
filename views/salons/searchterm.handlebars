<h1>Characters Found</h1>
<main>
    Hello
    <input type="text" id="search" />
        <button type="submit">Search Show
    {{!-- <form id="searchForm">
        <label for="search">Search Term</label>
        <input type="text" id="search" />
        <button type="submit">Search Shows</button>
        <div class="error" id="errorDiv"></div>
    </form> --}}

    <h2>{{this.demo.title}}</h2>
    {{#if data}}
    <ul>
        {{#each data}}
        <li>
            <a href="/name/{{this.id}}">{{this.name}}</a>
        </li>
        {{/each}}
    </ul>
    {{else}}
    <p class="not-found">We're sorry, but no results were found for {{this.demo.title}}.</p>
    {{/if}}


    {{#if hasErrors}}
    {{#each errors}}
    <li>
        {{this}}
    </li>
    <div class="another">
        <a href="/">Make another search</a>
    </div>
    {{/each}}
    {{/if}}
    <div class="another">
        <a href="/">Make another search</a>
    </div>

</main>